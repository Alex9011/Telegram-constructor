# Telegram Bot Constructor MVP

Простий MVP конструктора логіки Telegram-ботів на FastAPI.

## Що вміє

- Створення проєктів
- Редактор блоків: start, message, buttons, input, end
- Налаштування переходів між блоками
- Збереження схеми в SQLite (JSON)
- Симуляція сценарію в браузері
- Експорт JSON-схеми
- Регулярні автоматичні розсилки (daily / weekly)

## Вимоги

- Python 3.9+
- Windows PowerShell або CMD

## Встановлення та запуск

1. Перейди до папки проєкту:

   ```powershell
   cd C:\Users\1\OneDrive\Desktop\Telegram-constructor
   ```

2. Створи віртуальне оточення (якщо ще не створено):

   ```powershell
   python -m venv .venv
   ```

3. Активуй віртуальне оточення:

   ```powershell
   .venv\Scripts\activate
   ```

4. Встанови залежності:

   ```powershell
   pip install -r requirements.txt
   ```

5. Запусти сервер:

   ```powershell
   uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
   ```

6. Відкрий у браузері:

   http://127.0.0.1:8000

## Швидкий сценарій перевірки

1. На головній сторінці створи проєкт.
2. Перейди в редактор і додай блоки.
3. Натисни "Зберегти схему".
4. Відкрий "Симулятор" і пройди сценарій.
5. Перевір "Експорт JSON".

## Готовий demo-бот Барбершопа

1. На головній сторінці відкрий список "Мої проєкти".
2. У списку вже є готовий проєкт "Демо: OldBoy Style (Барбершоп)".
3. Перейди в "Симулятор" і пройди повний сценарій запису.
4. Після підтвердження заявки дані зберігаються в таблицю `booking_requests`.

У демо вже є:
- Головне меню (запис, послуги, барбери, адреса, контакти, FAQ)
- Розділ послуг і цін
- Картки барберів з переходом до запису
- Повний сценарій бронювання (ім'я, телефон, послуга, барбер, дата, час, коментар)
- Підсумок заявки та підтвердження

## Запуск реального Telegram-бота з сайту

1. У BotFather створи бота і скопіюй token.
2. Відкрий свій проєкт у браузері: `http://127.0.0.1:8000`.
3. Перейди в `Telegram` налаштування проєкту.
4. Встав token і натисни "Зберегти налаштування".
5. Увімкни бота кнопкою "Увімкнути бота".
6. Натисни "Запустити polling" прямо на цій же сторінці.
7. Напиши боту в Telegram: `/start`.

Щоб зупинити роботу бота, натисни "Зупинити polling" у тих самих налаштуваннях.

## Регулярні розсилки

1. Відкрий редактор проєкту.
2. Натисни "Автоматичні повідомлення".
3. Створи розсилку:
   - назва;
   - текст повідомлення;
   - тип розкладу (daily або weekly);
   - час відправки;
   - timezone (наприклад Europe/Kiev або UTC);
   - активність розсилки.
4. Для weekly додатково вкажи день тижня (0-6).

Розсилки запускає фоновий scheduler раз на хвилину.
Повторної відправки в той самий часовий слот немає: використовується поле `last_run_at`.

### Як протестувати локально

1. Запусти сервер (`uvicorn app.main:app --reload --host 127.0.0.1 --port 8000`).
2. Переконайся, що у проєкту налаштований і увімкнений Telegram-бот.
3. Створи розсилку на найближчу хвилину у потрібному timezone.
4. Дочекайся запуску scheduler (перевірка щохвилини).
5. Перевір повідомлення у Telegram і в операторських чатах (історія out-повідомлень).

### Що можна покращити далі

- Сегментація аудиторії (по тегах, активності, даті останньої взаємодії)
- Одноразові кампанії (single-run)
- Детальна статистика доставки (sent / failed / blocked)
- Попередній перегляд повідомлення перед запуском

## Структура проєкту

- app/main.py — запуск FastAPI застосунку
- app/models.py — SQLAlchemy моделі Project та Block
- app/routers/projects.py — сторінки та API редактора
- app/routers/simulator.py — API симулятора
- app/services/flow_engine.py — логіка валідації та виконання схеми
- app/templates/ — HTML шаблони
- app/static/ — CSS та JavaScript

## Примітка

Команда npm start для цього проєкту не використовується, бо це не Node.js застосунок. Запуск виконується через uvicorn.
